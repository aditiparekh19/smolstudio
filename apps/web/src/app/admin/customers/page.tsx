'use client';
import Link from 'next/link';import{useEffect,useState}from'react';import{apiClient}from'../../../lib/graphql';import{adminCustomersQuery}from'../../../lib/orders';import{useAuth}from'../../../components/AuthProvider';
type Row={id:string;email:string;phone:string|null;firstName:string|null;lastName:string|null;role:string;createdAt:string;orderCount:number;totalSpentInr:number};export default function Customers(){const{user,loading}=useAuth();const[rows,setRows]=useState<Row[]>([]);const[search,setSearch]=useState('');const[busy,setBusy]=useState(true);const[error,setError]=useState('');async function load(){setBusy(true);setError('');try{const r=await apiClient().request<{adminCustomers:Row[]}>(adminCustomersQuery,{search:search||undefined});setRows(r.adminCustomers)}catch(e){setError(e instanceof Error?e.message:'Unable to load customers.')}finally{setBusy(false)}}useEffect(()=>{if(!loading&&user)void load()},[loading,user]);if(loading)return <main className="mx-auto max-w-7xl px-5 py-16">Loading…</main>;if(!user||(user.role!=='ADMIN'&&user.role!=='STAFF'))return <main className="mx-auto max-w-7xl px-5 py-16">Admin access required.</main>;return <main className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><Link href="/admin" className="text-sm text-[#8b7a70]">← Back office</Link><h1 className="mt-2 font-serif text-5xl text-[#5e473c]">Customers</h1><p className="mt-2 text-[#8b7a70]">Customer accounts, order history and spend.</p><div className="mt-8 flex gap-3"><input value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>e.key==='Enter'&&void load()} placeholder="Search name, email or phone" className="flex-1 rounded-full border border-[#d9cbc0] bg-white px-5 py-3"/><button onClick={()=>void load()} className="rounded-full border border-[#cdbfb5] px-5 py-3">Search</button></div>{error&&<p className="mt-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</p>}<div className="mt-6 overflow-hidden rounded-4xl border border-[#eadfd5] bg-white">{busy?<p className="p-8 text-[#8b7a70]">Loading customers…</p>:rows.length===0?<p className="p-8 text-[#8b7a70]">No customers found.</p>:<div className="divide-y divide-[#eadfd5]">{rows.map(c=><Link key={c.id} href={`/admin/customers/${c.id}`} className="flex flex-wrap items-center gap-4 p-5 hover:bg-[#fffaf4]"><div className="min-w-[220px] flex-1"><p className="font-medium text-[#5e473c]">{[c.firstName,c.lastName].filter(Boolean).join(' ')||'Customer'}</p><p className="text-xs text-[#8b7a70]">{c.email}{c.phone?` · ${c.phone}`:''}</p></div><span className="text-sm">{c.orderCount||0} orders</span><strong>₹{Number(c.totalSpentInr||0).toLocaleString('en-IN')}</strong></Link>)}</div>}</div></main>}
